
export const metadata = { title: "Syntech Solutions", description: "ImmersiFair — VR + Haptics + Employability" };
import "./globals.css";
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="en"><body>{children}</body></html>);
}
